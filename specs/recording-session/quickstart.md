# Quickstart: 記録画面

1. アプリで「記録」タブを開き、日付を選択する（デフォルトは今日）。
2. 「種目を追加」で筋トレメニューを複数選択する。選択後、各種目のセット重量・回数を入力し、「セットを追加」で行を増やす。
3. ランニング距離(㎞)と経過時間(分:秒)を入力すると自動でペースが更新される。
4. 「記録を開始」をタップすると開始時刻が保存され、ヘルスケアデータ取得が始まる。取得状況はサマリーカードで確認できる。
5. 内容を確認し「保存」で`TrainingLog`が生成される。保存後は前画面へ戻す想定。
